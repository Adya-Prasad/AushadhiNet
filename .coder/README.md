# Running AushadhiNet-GATv2 model in Coder
# Running AushadhiNet-GATv2 in Coder

Running the AushadhiNet drug interaction prediction system in a Coder workspace.

### Step 1: Install Dependencies
Dependencies should install automatically from `requirements.txt`. If not, run:
```bash
pip install -r requirements.txt
```
